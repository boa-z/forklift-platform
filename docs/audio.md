# 提示音与报警语音

复刻参考工程的语音体系：14 段提示/报警语音（多语言）+ 开机音 + 按钮音，
由报警引擎按门限与节拍触发。

## 1. 语音清单（参考工程 `coustom/voice/`）

| 编号 | 文案 | 触发 | 语言 |
| --- | --- | --- | --- |
| 01 | 请刷卡 | 授权屏（未实现，离线占位） | 10 种 |
| 02 | 刷卡成功请安全驾驶 | 授权屏 | 10 种 |
| 03 | 刷卡失败请联系管理员 | 授权屏 | 10 种 |
| 04 | 车辆故障请检查 | 存在活动故障，5 秒一次 | 10 种 |
| 05 | 倒车请注意 | 倒车（参考在雷达处理 ≥150mm 或无探头分支） | 10 种 |
| 06 | 请系好安全带 | 手刹松开且未系安全带，5 秒一次 | 10 种 |
| 07 | 检测到操作员离座请回到驾驶位置 | 手刹松开且离座，5 秒一次（与 06 交替） | 10 种 |
| 08/09/10 | 超速三档提示 | 速度 > 基准（默认 5）× 12/13/15 | 10 种 |
| 11/12 | 后方障碍物接近 / 距离较近请减速 | **参考代码当前未调用**（仅素材保留） | 10 种 |
| 13 | 危险注意刹车 | 倒车雷达 < 150mm | 10 种 |
| 14 | 即将发生碰撞停车停车 | 倒车雷达 < 50mm | 10 种 |
| — | 开机音（`audio/app_start_up.wav`） | 启动一次 | — |
| — | 按钮音（`audio/btns.wav`） | 每次按键 | — |

多语言文件名后缀：英/俄/法/德/韩/葡/西/阿/日；无后缀为中文。
**全部 10 种语言已迁移**（14×10 = 140 段），文件名 `voice-XX-<lang>.wav`。

## 2. 产品实现

- **协议 v4**：`SoundId` 追加语义编号（cardSwipe/cardOk/cardFail/overspeed/
  overspeedHigh/overspeedCritical/seatbelt/seatOff/obstacle*/collision）。
- **报警引擎**（`ui/src/platform/alarms.ts`）：按参考的 50ms 拍推进；
  超速逐拍请求（三档）、倒车逐拍请求、故障与安全带/离座 5 秒一次
  （交替类型）；请求顺序即优先级（超速 → 倒车 → 故障 → 安全带/离座）。
- **语音控制器**（`ui/src/platform/voice.ts`）：单播放器；报警语音播放中丢弃
  新请求（参考 `ThreadPlayer_AudioPlay` 返回 -2 的语义），按钮/开机音可打断；
  语言后缀按当前界面语言选择，缺失回退中文。
- **资产**：`tools/make-voice-assets.sh` 迁移全部 10 种语言并重采样（参考为
  24000 Hz，PocketJS 规范只允许 44.1/22.05/11.025k），随后重写 `ui/pak.json`
  的 142 个 `audio:wav.*` 原始块（140 语音 + 开机音 + 按钮音）。
  **采样率按设备存储分配**：中文与英文（界面可切换）22050 Hz；
  其余 8 种语言 11025 Hz（完整覆盖，电话级语音质量）；开机音 44100→22050 Hz。
  依据：设备 rootfs 54MB、全语言 24kHz 约 30MB；当前方案语音共 ~16MB，
  全语言 pak 21.7MB，部署后 rootfs 仍剩 ~17MB。
- **播放**：PocketJS 宿主 audio 模块（d211 用 `aplay -t raw` 子进程 + 设备时钟
  节拍）；宿主未挂载 audio 模块时静默降级（开发/模拟）。

## 3. 与参考的差异

- 雷达语音（05/13/14）暂按「倒车即播 05」处理：平台还没有超声波雷达数据
  （M3 接入探头后按 <50/<150 门限切 14/13）。
- 授权屏语音（01/02/03）随授权屏一起实现（当前为离线占位）。
- 参考的按钮音与语音可能是两条播放通路；本实现为单播放器（框架播放器的
  poll 是模块级队列，两个播放器会互相消费 credit），按钮音打断语音。
- 参考的语音是 24000 Hz 原始文件；本实现重采样到 22050 Hz（语音质量影响可忽略）。

## 4. 设备验证（D211）

- 开机：`audio createStream 44100 ch=2` → 开机音播放。
- 超速场景（mock 80 km/h）：连续循环播放 10 号语音，间隔 3.2 秒（与剪辑
  时长 3.19 秒一致）。
- 故障场景（mock faultId 4）：每 5 秒一次 4 号语音。
- 倒车场景：1.8 秒循环（中文 05 号语音时长 1.82 秒）。
- 亮度回归：设置滑条写 `/sys/class/backlight`。
- 多语言：英文模式超速循环间隔 4.87s（= `voice-10-en` 时长，中文为 3.19s）；
  强制俄语后缀时宿主日志出现 `createStream rate=11025`（`voice-10-ru` 加载成功）。
- 存储：全语言 pak 21.7MB 部署后 `df` 显示 rootfs 使用 36.2MB / 剩余 17.7MB。

宿主侧实现与修复（credit 合并上报、feeder 设备时钟节拍、aplay 150ms 缓冲）
见 PocketJS `feat/d211-host-modules`（PR #417）。
