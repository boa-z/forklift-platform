{
  "name": "@forklift/ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "check": "bunx tsc --noEmit && bunx tsc --noEmit -p tsconfig.ui.json"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  }
}
